apiVersion: v1
kind: Service
metadata:
  name: flask-app-Service
spec:
   type: NodePort
   selector:
      app: devops-python-app
   ports:
   - protocol: TCP
     port: 80
     targetPort: 5000
     nodePort: 30007